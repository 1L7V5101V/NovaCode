Embedding 检索是把 query 和每条笔记分别通过一个神经网络模型（如 text-embedding-ada-002）转换成向量（一组浮点数），然后用余弦相似度等距离度量做 **语义相似度排序**，即使 query 和笔记一个词都不重叠，只要语义相近也能召回。

相比之下，当前代码的 tag/关键词/时间排序完全不依赖模型，结果可解释性强，但无法处理"同义不同词"的情况（比如 query 说"天气"，笔记里只有"下雨"但没有"天气"这个词，就召不回来）。