12 个记忆依赖任务定义在 `pico/metrics.py:330-343`，分为 **3 类 × 4 个**：

**fact_lookup（事实查找）**
1. `fact_color` — deploy key is red
2. `fact_api` — api base path is /v1/internal
3. `fact_budget` — default step budget is 6
4. `fact_timeout` — timeout ceiling is 120 seconds

**edit_dependency（编辑依赖）**
5. `edit_intro` — first bullet is the locked intro line
6. `edit_token` — second token is placeholder
7. `edit_field` — fixed field name is benchmark_schema
8. `edit_line` — locked marker is on line three

**history_reference（历史引用）**
9. `history_file` — deploy fact came from facts.txt
10. `history_line` — benchmark note came from line two
11. `history_token` — placeholder token was beta
12. `history_tool` — inspection tool was read_file

实验在 `memory_on` / `memory_off` / `memory_irrelevant` 三种变体下对比，衡量重复读取、工具步数和正确率。