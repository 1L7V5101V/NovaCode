12 组配置定义在 `pico/metrics.py:438` 的 `run_context_stress_matrix()`，是 **3 × 2 × 2** 组合矩阵：

| 维度 | 级别 |
|------|------|
| **历史长度 (history)** | short (4 条), medium (12 条), long (24 条) |
| **记忆负载 (notes)** | low (2 条), high (10 条) |
| **请求长度 (request)** | short, long |

组合出的 12 个 config ID：

| | 配置 |
|---|------|
| 1 | `short-low-short` |
| 2 | `short-low-long` |
| 3 | `short-high-short` |
| 4 | `short-high-long` |
| 5 | `medium-low-short` |
| 6 | `medium-low-long` |
| 7 | `medium-high-short` |
| 8 | `medium-high-long` |
| 9 | `long-low-short` |
| 10 | `long-low-long` |
| 11 | `long-high-short` |
| 12 | `long-high-long` |

设计思路见面试话术文档 `:258-289`：不是单变量问题，通过三档变量组合系统性覆盖历史长度、记忆负载、请求长度几个主要压力维度。结果平均 prompt 从 7082 压缩到 5664（~16.19%），当前请求不被裁剪。

---
**历史长度 (history)** — 注入的 fake message 条数：
- `short` = 4 条，每条 content 为 `"matrix-history-{index}-" + "B" * 220`
- `medium` = 12 条，同上格式
- `long` = 24 条，同上格式

**记忆负载 (notes)** — 注入的 episodic note 条数：
- `low` = 2 条，每条 content 为 `"matrix-note-{index}-" + "A" * 180`
- `high` = 10 条，同上格式

**请求长度 (request)** — agent 收到的 user message 文本：
- `short` = `"recall"`
- `long` = `"recall the relevant benchmark fact without dropping the latest request details"`

（来源 `pico/metrics.py:440-442`，`run_context_stress_matrix()`，模拟模型实验；真实模型实验在 `:918-923` 请求文本不同但结构一致。）